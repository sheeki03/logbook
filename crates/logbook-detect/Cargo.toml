[package]
name = "logbook-detect"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Anomaly / risk detection rule engine over the unified event stream (Phase 3): secret-in-diff, dangerous shell, risky git, non-allowlisted egress, token/cost spikes, tool-call rate — emits Kind::Finding / Category::Security events."

[dependencies]
logbook-core = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
